datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model dahuaEvent {
  id      Int    @id @default(autoincrement())
  message String @db.Text
}